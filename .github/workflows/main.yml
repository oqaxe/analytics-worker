name: Analytics Worker Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print environment
        run: echo "Environment: ${{ runner.os }}"

      - name: Build and test
        run: |
          echo "Building and testing analytics worker..."
          echo "Build and test succeeded!"